{
  "name": "worldwide-fm",
  "version": "7.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild app.jsx --bundle --outfile=bundle.js --loader:.jsx=jsx --platform=browser"
  },
  "dependencies": {
    "esbuild": "^0.20.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
